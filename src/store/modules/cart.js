const state = {
  cartInfo: [],
};

const getters = {
    inCart: (state) => state.cartInfo
};

const actions = {
    
};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
